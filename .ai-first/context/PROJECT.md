# Project

`ai-first` is a small local memory and maintenance workspace for AI-assisted
repositories.

The owner keeps working with agents normally. `ai-first` helps by giving agents
durable project context, active work, backlog, ideas, changelog history, and
project history that survive fresh chats.

`.ai-first/context` is the project memory source of truth.
