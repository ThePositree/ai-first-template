# In Progress

Only active work belongs here.

## Bootstrap

**What:** Initialize the project from the owner's free-form description.

**Why now:** The template has been copied but the product context is empty.

**Expected gain:** Agents can start future work from filled product, task, and
architecture docs instead of guessing.

**Acceptance:** Product docs are filled, initial roadmap/backlog are created,
`CHANGELOG.md` has the first real entry, `.bootstrap/FIRST_RUN.md` is removed,
and the `First Run Bootstrap` section is removed from `AGENTS.md`.

**Links:** `.bootstrap/FIRST_RUN.md`, `AGENTS.md`,
`docs/product/PROJECT_BRIEF.md`.
