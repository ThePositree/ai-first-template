# AGENTS.md - ai-first source repository

This repository builds `ai-first`.

GitHub is the source of truth for project memory, planning, ideas, decisions, and open questions in this repository. Use the repository Wiki, Discussions, Issues, and Project `ai-first` before making product or planning assumptions.

Do not recreate a project-local dogfooding memory workspace in this repository.

The installable envelope lives under `envelope/files`; do not change it unless the owner explicitly asks for product/envelope work.

Do not touch `site` unless the owner explicitly asks for site work.

The working tree may contain unrelated owner changes. Do not revert them unless the owner explicitly asks.
